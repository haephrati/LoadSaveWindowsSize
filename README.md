# LoadSaveWindowsSize

MFC sample that persists child-window sizes. Open the Visual C++ project in the matching Visual Studio version.

## Unit tests

`Tests/WindowSizeTests` checks about, mainframe, and window-document-type resource IDs. Build Release|x64.